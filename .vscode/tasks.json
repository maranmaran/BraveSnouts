{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "command": "npx",
            "type": "shell",
            "args": [
                "tsc"
            ],
            "options": {
                "cwd": "${workspaceFolder}/src/utilities"
            },
            "problemMatcher": []
        }
    ]
}